export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface ITravelInfo {
  icon: string;
  title: string;
  departure: string;
  arrival: string;
  date: string;
  time: string;
  seat: string;
  ticketInfo: string;
  notice: string;
}
